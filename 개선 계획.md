> **확정 및 후속 정리:** 아래 초안 논의를 거쳐 A/B/C 3버전 구도로 확정했다. 최신 버전 구성, B버전이 langgraph를 쓰지 않는 이유, 체크포인트 상태 기계 설계, 단계별 진행 상태는 `기획서.md` 0장·6장·7장·8장에 정리되어 있으니 그쪽을 최신 기준으로 참고한다. 이 문서는 최초 논의 초안으로 남겨둔다.

### 계획 요약
- A버전은 V1.0.2에 있는 대로 md 파일만 넣어서 웹에서 바로 쓸 수 있게 유지
- B버전은 지금 있는 프로젝트에서 아래 내용들을 개선해서 고도화 (단, 실제 LLM 호출은 계속 클로드 구독 기반 VSCode 확장이 담당하므로 langgraph 등 오케스트레이션 라이브러리는 도입하지 않는다 — 개념만 차용해 stdlib 체크포인트 상태 기계로 구현)
- C버전은 별도로, Setuk-AI가 직접 API 키로 LLM을 호출하는 트랙으로 분리 (로드맵 단계, `기획서.md` 8장 참고)

### **단일 LLM에서 멀티 에이전트 시스템으로의 진화**

초기 Setuk-AI는 `skill.md` 파일들만 제공하는 단순한 하네스 스킬 프로젝트로 개발되었다. 그러나 배포 후 선생님들을 대상으로 한 설문 피드백 결과, "생성되는 세특 결과물의 퀄리티와 전문성이 더 깊었으면 좋겠다"는 강력한 의견이 접수되었다.

이를 해결하기 위해 기존의 가볍고 빠른 단일 모델 방식을 'A 버전'으로 남겨두고, 시스템 내부적으로 특화된 여러 에이전트가 협업하는 **멀티 에이전틱 시스템을 적용한 'B 버전'을 새롭게 기획**하여 개발했다. LangGraph의 슈퍼바이저 패턴을 도입해 데이터를 리서치하는 에이전트, 학생의 성향을 분석하는 에이전트, 최종 문문을 다듬는 에이전트로 역할을 분담시킨 뒤, 사용자가 자신의 필요에 따라 속도 중심의 A버전 vs 퀄리티 중심의 B버전을 선택할 수 있도록 아키텍처를 고도화하고 사용자 중심의 서비스를 완성했다.

### **LangGraph 기반 Human-in-the-Loop 적용**

기존의 세특 생성 로직은 AI가 사용자의 입력을 받으면 처음부터 끝까지 최종 결과물을 단번에 완성해 버리는 제로샷(Zero-shot) 구조였다. 하지만 설문조사 데이터를 분석한 결과, 선생님들이 AI가 만들어준 최종 결과물을 그대로 사용하지 않고 본인의 의도나 학생의 세부 특성에 맞게 "결과물을 다시 중간에서 수정하는 빈도"가 압도적으로 높다는 실무적 한계를 발견했다.

이러한 문제를 소프트웨어 엔지니어링으로 해결하기 위해 워크플로우 내에 인간의 개입을 허용하는 **Human-in-the-Loop** 체크포인트를 전격 도입했다. LangGraph의 Interrupt와 State 저장 기능을 활용하여, AI가 Draft를 완성하는 단계에서 그래프 실행을 일시 중지시키고 선생님에게 검토를 요청한다. 선생님이 초안을 수정하거나 승인을 내리면, 저장된 상태에서부터 정확히 중단된 지점을 재개하여 최종 포맷팅 작업을 완료하는 구조를 구현했다.  AI의 통제권과 사용자 개입 문제를 조율한 아키텍처를 완성하여 호평을 받았다.