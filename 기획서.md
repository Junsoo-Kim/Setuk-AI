# 프로젝트 기획서: 세특 작성 보조 AI (A/B/C 3버전 구성)

## 0. 버전 구성 개요

이 프로젝트는 하나의 앱이 아니라, 사용 환경과 비용 부담이 다른 세 개의 배포물로 나뉜다. 교사는 자신의 환경(구독 여부, 설치 가능 여부)에 맞는 버전 하나만 받아 쓰면 된다.

**버전 간 의존 관계(정정, 2026-09-10).** 이 문서는 오랫동안 "세 버전은 서로의 파일을 참조하지 않는다"고 적어 왔으나 실제 코드와 달랐다. 정확한 현재 구조는 다음과 같다.

| 버전 | 자기완결성 | 실제 참조 |
| --- | --- | --- |
| A | 완전 자기완결 | 없음 |
| B | 완전 자기완결(배포 ZIP 그대로) | 없음 |
| C | **B에 의존** | `.agent/*.md`, `linter.py`, `rules.json`, `scripts/extract_docx.py`, 데이터 폴더(`학생정보/`·`보고서/`·`세특/`) |

C가 B를 참조하는 것은 실수가 아니라 의도한 설계다. 세특 판정 기준(분량·금칙어·허용 문자·지침)이 두 갈래로 갈라지면 같은 학생 자료로 두 버전이 다른 결론을 내게 된다. 그래서 C는 자체 사본을 두지 않고 `version_C/bridge.py`가 `version_B/`를 기준 경로로 잡아 그대로 import한다.

의존 방향은 **C → B 단방향**이다. B는 C의 존재를 모르므로 C 쪽 변경이 B 배포 ZIP에 영향을 주지 않는다. 다만 그 역은 성립하지 않는다 — **B의 지침이나 `rules.json`을 고치면 C의 결과도 함께 바뀐다.** C의 각 실행(run)에 `prompt_version`(지침 파일 묶음의 해시)을 기록하는 이유가 이것이다.

| | A버전 | B버전 | C버전 |
| --- | --- | --- | --- |
| 실행 환경 | 웹 (Claude Project 등 지식 베이스 업로드) | 로컬 VSCode + Claude Code(또는 Cline/Codex) | 로컬 또는 서버, 자체 API 클라이언트 |
| LLM 호출 주체 | 웹 채팅 자체 | VSCode AI 확장(교사의 클로드 구독) | Setuk-AI 코드가 API 키로 직접 호출 |
| 설치 | 불필요 (md 파일 업로드) | 무설치 ZIP (포터블 Python 동봉) | pip 설치 + API 키 발급 필요 |
| 비용 | 웹 채팅 요금제 안에서 해결 | 클로드 구독료 외 추가 비용 없음 | 구독료 + API 종량 과금 |
| 산출물 품질/유연성 | 가장 간단, 다중 보고서·창체 등 세분화 모드 없음 | 준수한 품질, 세특·창체·물리 통합 등 다중 모드 지원 | 가장 정교함 목표 (역할 분리 멀티에이전트 + 사람 개입 검토) |
| 현재 상태 | **완료, 동결** (`version_A/` 폴더) | **`version_B/` 폴더, 고도화 진행 중** | **v1 구현 완료** (`version_C/`, Phase 8 파일럿 선행 없이 착수) |

- A버전은 `version_A/` 폴더 안에서 그대로 동결한다. 이후 루트 작업(B버전 고도화)은 이 폴더를 수정하지 않는다. 동결 시점은 git 태그 `v1.0.2-a-frozen`으로 고정했다.
- 이 문서의 1~5장은 모두 **B버전(`version_B/` 폴더)의 아키텍처**를 설명한다.
- 6장은 B버전 고도화(멀티에이전트 화 + Human-in-the-Loop) 로드맵이다.
- 7장은 A버전 요약, 8장은 C버전 로드맵이다.

> 참고: 세 버전은 저장소 루트 바로 아래 `version_A/`·`version_B/`·`version_C/`로 각각 독립된 폴더에 있다(2026-09-04 재구성, 10장 참고). `version_B/VERSION` 파일(`0.3.0`)은 B버전 자체의 배포 버전이다.

---

## 1. 프로젝트 개요 (B버전 기준)

- **프로젝트명:** 로컬 환경 기반 AI 세특 작성 파이프라인 (가칭: Setuk-Harness B)
- **목적:** 고등학교 교사들의 학기 말 학교생활기록부 세부능력특기사항 작성 업무 부담 경감 및 기재 품질 상향 평준화
- **타겟 사용자:** 클로드(또는 동급 AI 확장) 구독은 있지만 별도 API 키 발급·과금은 부담스러운 고등학교 교사 (개발 지식 불필요)
- **기술 스택:**
    - 실행 환경: 로컬 VSCode 및 내장 AI 익스텐션 (Cline, Codex, Claude Code 등)
    - 검증 환경: Portable Python (설치 불필요)
- **배포 방식:** 템플릿과 포터블 파이썬이 모두 포함된 ZIP 압축 파일 배포 (사용자는 압축 해제 후 즉시 사용)

## 2. 기대 효과

- **API 비용 및 토큰 소모량 대폭 절약:**
    - 모듈화된 `.agent` 디렉토리 구조는 AI가 각 단계마다 필요한 지침만 메모리에 올리게 하므로, 거대한 단일 프롬프트를 사용할 때보다 컨텍스트 토큰 소모를 크게 줄입니다.
    - 산문 대신 YAML 구조를 사용하여 입력 토큰 비용을 최소화합니다.
- **무설치(Zero-Setup) 환경의 뛰어난 접근성:** 포터블 파이썬을 내장 배포하여, 학교 PC에 파이썬을 설치하거나 환경 변수를 세팅하는 복잡한 과정 없이 누구나 즉시 사용할 수 있습니다.
- **데이터 무결성 확보:** AI의 창의적 산출물을 파이썬 Linter로 강력하게 통제하여 글자 수 초과나 금칙어로 인한 나이스 시스템 반려 문제를 원천 차단합니다.
- **로컬 파일 기반 보안성:** 학생의 활동 내역과 결과 파일은 별도 Setuk-Harness 서버나 데이터베이스가 아니라 교사의 로컬 PC에 저장됩니다. 단, VSCode AI 확장 기능과 연결된 모델 제공자가 처리를 위해 입력 내용을 외부로 전송할 수 있으므로, 학교·교육청의 개인정보 기준과 해당 서비스의 데이터 처리 설정을 별도로 확인해야 합니다.

## 3. 아키텍처 및 핵심 워크플로우 (B버전 현재 구현 기준)

별도의 환경 설정이나 서버 구축 없이, 로컬 VSCode 내에서 AI 에이전트와 정적 분석기가 상호작용하는 자동화 파이프라인입니다.

### A. 에이전트 모듈화 파이프라인 (`.agent` 디렉토리 기반)

단일 프롬프트의 복잡성을 줄이고자, 공통 마스터 설정 파일(`AGENTS.md`)과 Cline 전용 규칙 디렉토리(`.clinerules/`)가 여러 개의 에이전트 지침을 순차적으로 호출하여 작업을 수행합니다. (세특 4단계 외에도 창체·물리 통합·땜빵모드가 각자의 지침 세트를 가진다. 상세 흐름은 `.agent/README.md` 참고.)

1. **보고서 입력 에이전트 (01_report_ingestion.md 등):** 지정 학생의 DOCX 보고서를 읽고 사실·실험 한계·역량 근거를 `학생정보/<학생명>.yaml`로 정리.
2. **전처리 에이전트 (02_data_structuring.md):** YAML 키워드를 분석하여 인과관계가 명확한 '문장형 명제'로 1차 가공 (환각 방지).
3. **작성 에이전트 (03_drafting.md 등):** [학습 동기] -> [탐구 활동] -> [학업 역량 및 성장]의 3단계 구조를 갖춘 초안 작성.
4. **평가 에이전트 (04_evaluation.md):** 입학사정관 시각에서 초안의 내용적 깊이와 교사 평가성을 자체 검토 및 튜닝하여 마크다운 파일로 저장.

### B. 무설치 정적 분석 및 자율 피드백 (Portable Python Linter)

- **동작 방식:** 에이전트가 세특 작성을 완료하면, 배포 패키지에 내장된 포터블 파이썬을 활용해 터미널에서 정적 분석 스크립트를 자동 호출합니다. (예: `.\python_portable\python.exe linter.py 세특/학생명.md`)
- **검사 항목:** 나이스(NEIS) 시스템 기준 바이트 수 초과 여부, 특수기호 포함 여부, 교육부 금칙어 포함 여부.
- **자율 피드백 루프:** Linter 실행 결과 오류가 발생하면, AI가 터미널의 에러 로그를 읽고 마크다운 파일을 스스로 수정한 뒤 재검사합니다. 오류가 0이 될 때까지 이 과정을 반복합니다.

## 4. 디렉토리 및 파일 구조 계획

```
Setuk-Harness/
├── AGENTS.md                    # Codex/Cline 공통 마스터 규칙 (에이전트 순서 및 Linter 실행 규칙)
├── .clinerules/                 # Cline 전용 보완 규칙 및 선택형 워크플로
│   ├── 00-setuk-master.md
│   └── workflows/
│       └── setuk.md
│
├── .agent/                      # 각 에이전트별 독립된 지침 보관소
│   ├── 01_report_ingestion.md
│   ├── 02_data_structuring.md
│   ├── 03_drafting.md
│   └── 04_evaluation.md
│
├── 보고서/                      # 학생별 DOCX 원본을 넣는 로컬 입력 폴더
│   └── README.md
├── 학생정보/                    # 교사가 학생 데이터를 입력하는 폴더
│   └── template.yaml            # 평가 표준 동사 리스트가 주석으로 포함된 양식
├── 세특/                        # 검증이 완료된 최종 세특 파일 저장소 (.md)
│
├── python_portable/             # 윈도우용 포터블 파이썬 런타임 (사용자 설치 불필요)
├── scripts/
│   └── extract_docx.py          # DOCX 문단·표 UTF-8 JSON 추출기
├── linter.py                    # 정적 분석 메인 스크립트
└── rules.json                   # 금칙어 리스트 및 나이스 글자 수 검사 기준
```

## 5. 상세 유저 플로우 (User Flow)

교사는 파이썬 설치나 복잡한 명령어 타이핑 없이 시각적인 승인 절차만 거치게 됩니다.

1. **프로젝트 열기 및 데이터 입력:**
    - 압축 파일을 풀고 VSCode에서 폴더를 엽니다. (최초 1회 '작업 영역 신뢰' 승인)
    - 학생 DOCX를 `보고서/`에 넣어 AI가 `학생정보/김준수.yaml`을 생성하게 하거나, `학생정보/template.yaml`을 복사하여 직접 키워드를 기입합니다.
2. **파이프라인 실행 지시:**
    - VSCode AI 채팅창에 다음과 같이 간단히 입력합니다.
    - *"김준수 세특 작업 시작해"*
3. **AI 자율 생성 및 저장:**
    - AI가 마스터 지침을 읽고 `.agent/` 폴더의 규칙에 따라 보고서 입력을 포함한 4단계 과정을 내부적으로 수행합니다.
    - 완료된 결과물을 `세특/김준수.md`에 자동 저장합니다.
4. **Linter 자동 실행 및 승인 (Human-in-the-loop):**
    - AI가 터미널 명령어(`./python_portable/python.exe linter.py 세특/김준수.md`)를 준비하고 교사에게 실행 허용(Approve)을 요청합니다.
    - 교사가 'Approve' 버튼을 클릭하면 정적 분석이 시작됩니다.
    - 에러 검출 시 AI가 스스로 파일을 수정하고 다시 승인을 요청하며, 에러가 없을 때까지 튜닝을 반복합니다.
5. **나이스(NEIS) 제출 및 세션 초기화:**
    - 에러가 0이 되면, 교사는 `세특/김준수.md` 본문을 복사하여 나이스 시스템에 즉시 붙여넣고 저장합니다.
    - 학생 간 정보 혼선(할루시네이션)을 방지하기 위해 현재 채팅 세션을 닫고 새 채팅(New Chat)을 연 뒤 다음 학생 작업을 준비합니다.

---

## 6. B버전 고도화 로드맵

### 6.1 배경

배포 후 설문 피드백에서 두 가지가 확인되었다.

1. "생성되는 세특 결과물의 퀄리티와 전문성이 더 깊었으면 좋겠다" — 단일 모델·제로샷 방식의 한계.
2. 교사가 AI 초안을 그대로 쓰지 않고 **중간에 직접 수정하는 빈도가 매우 높다** — 처음부터 끝까지 한 번에 완성해 버리는 현재 흐름은 이 실무 패턴과 맞지 않는다.

이를 해결하기 위해 (1) 역할이 분리된 에이전트 구조(리서치/성향분석/윤문)로 정교화하고, (2) 초안 완성 직후 실행을 멈추고 교사 검토를 받은 뒤 재개하는 **Human-in-the-Loop 체크포인트**를 도입한다.

### 6.2 설계 원칙 — langgraph를 쓰지 않는 이유

B버전은 **클로드 구독만으로 동작**해야 한다는 제약이 있다. 즉 실제 문장을 생성하는 주체는 여전히 VSCode AI 확장(교사가 이미 구독 중인 Claude Code 등)이며, Setuk-AI 저장소의 코드는 그 확장을 프로그래밍적으로 호출하지 않는다.

`langgraph` 같은 오케스트레이션 라이브러리는 "그래프의 각 노드가 코드에서 직접 LLM 클라이언트를 호출한다"는 것을 전제로 한다. B버전에는 코드가 붙잡고 있는 모델 클라이언트가 없으므로, 이 라이브러리를 pip 의존성으로 추가해도 실질적으로 쓸 수 있는 기능이 없다. 또한 무설치 ZIP·포터블 Python(표준 라이브러리만 사용) 원칙과도 맞지 않는다.

따라서 B버전은 "슈퍼바이저 패턴"과 "Human-in-the-Loop"의 **개념만** 가져오고, 구현은 다음 세 가지로 대체한다.

1. **역할 분리된 지침 파일** — 이미 `.agent/02(리서치/구조화) → 03(초안=성향분석 반영) → 04(윤문/평가)`로 나뉜 계약 기반 구조를 유지·강화한다. (신규 코드 불필요, 지침 문서 정리 수준)
2. **stdlib 전용 체크포인트 상태 파일** — 파이썬 표준 라이브러리만으로 만든 경량 상태 기계가 진행 단계를 JSON으로 기록한다. (아래 6.3)
3. **별도 로컬 리뷰 화면** — 초안이 나오면 별도 CLI(우선) 또는 최소 로컬 웹 화면에서 교사가 직접 수정·승인한 뒤에만 다음 단계로 재개된다. (Phase 3에서 구현 예정, 요구사항은 6.4)

이 원칙에 따라 C버전(8장)에서만 실제 `langgraph`/Anthropic API 직접 호출을 도입한다.

### 6.3 체크포인트 상태 기계 설계 (Phase 2 산출물)

**저장 위치:** `run_state/<식별자>.json` (신규 폴더). `학생정보/*.yaml`과 마찬가지로 학생 식별 가능한 초안 본문을 담으므로 `.gitignore`에 `run_state/*.json`을 추가해 추적하지 않는다. (`학생정보/`, `세특/`과 동일한 보호 수준)

**단계(status) 값:**

| status | 의미 | 이 상태에서 생성/보유하는 것 |
| --- | --- | --- |
| `REPORT_INGESTED` | 01단계 완료 | `yaml_path` |
| `STRUCTURED` | 02단계 완료 | `structured_facts` 요약(경로 또는 요약 해시) |
| `DRAFTED` | 03단계 완료, 아직 검토 전 | `draft_text`, `draft_path` |
| `AWAITING_REVIEW` | 교사 검토 대기 (인터럽트 지점) | 위와 동일 + `interrupt_reason` |
| `REVIEWED` | 교사가 리뷰 화면에서 승인(또는 수정 후 승인) | `reviewed_text`, `reviewer_note`(선택) |
| `EVALUATED` | 04단계(윤문/평가) 완료 | `output_path` |
| `LINTED_PASS` / `LINTED_FAIL` | Linter 실행 결과 | `linter_report` 요약, 재시도 횟수 |

**최소 스키마 예시:**

```json
{
  "schema_version": 1,
  "student_key": "<식별자>",
  "mode": "A|B|C|D|E|F",
  "status": "AWAITING_REVIEW",
  "updated_at": "2026-09-04T12:00:00+09:00",
  "steps": {
    "report_ingestion": { "done": true, "yaml_path": "학생정보/김준수.yaml" },
    "data_structuring": { "done": true },
    "drafting": { "done": true, "draft_text": "...", "draft_saved_path": null },
    "review": { "done": false, "approved": false, "edited_text": null },
    "evaluation": { "done": false, "output_path": null },
    "lint": { "done": false, "pass": null, "retry_count": 0 }
  }
}
```

**interrupt 지점:** `03_drafting.md`(또는 03c/03e) 완료 직후, `04_evaluation.md` 실행 전으로 고정한다. 이미 `AGENTS.md`가 "초안 저장 후 Linter 승인을 받는다" 구조를 갖고 있어, 그 승인 지점을 앞으로 당겨 확장하는 정도이므로 기존 흐름과 자연스럽게 이어진다.

**재개 규칙:**
- `status == AWAITING_REVIEW`이고 리뷰 화면에서 `approved: true`가 기록된 경우에만 04단계로 재개한다.
- 그 외 상태에서 04단계를 재개하려는 시도는 거부하고, 현재 `status`를 그대로 보고한다.
- 교사가 리뷰 화면에서 초안을 수정했다면 `steps.review.edited_text`를 04단계의 입력으로 사용하고, `steps.drafting.draft_text`는 원본 보존용으로만 남긴다(덮어쓰지 않음).

**기존 마스터 규칙과의 충돌 및 예외 처리:** `AGENTS.md`의 "중간 계약은 현재 채팅 컨텍스트에서만 전달하고 파일로 저장하지 않는다"는 규칙은 이 체크포인트 파일에 대해 **명시적 예외**가 필요하다. `run_state/*.json`은 (a) `.gitignore`로 추적 제외, (b) `EVALUATED` 상태 도달 후 삭제 또는 최소 정보로 축소, (c) 최종 본문 외 학생정보를 복제하지 않는다는 원칙 유지(파일명에 학생 식별자를 쓰되 본문 외 개인정보는 넣지 않음)를 조건으로 허용한다. 이 예외 조항은 Phase 3(리뷰 화면)·Phase 4(지침 재구성) 착수 시 `AGENTS.md`에 실제로 반영한다 — 아직 체크포인트 파일을 만드는 코드가 없으므로 이번 단계에서는 설계만 기록한다.

### 6.4 로컬 리뷰 화면 (Phase 3에서 구현 완료)

- CLI 리뷰 도구로 구현했다. `scripts/run_state.py`(체크포인트 읽기·쓰기·검증·전이 규칙 강제)와 `scripts/review_cli.py`(교사용 open/approve/reject/status 명령)로 나뉘며, 둘 다 표준 라이브러리만 사용하고 LLM을 호출하지 않는다.
  - `review_cli.py open <식별자>`: `run_state/<식별자>.json`이 `AWAITING_REVIEW`일 때만 초안을 `run_state/<식별자>_review.md`로 꺼내 준다. 교사는 이 파일을 VSCode에서 직접 열어 고친다.
  - `review_cli.py approve <식별자> [--note ...]`: 검토 파일 내용을 그대로 승인 본문(`steps.review.edited_text`)으로 저장하고 status를 `REVIEWED`로 바꾼 뒤 검토 파일을 삭제한다.
  - `review_cli.py reject <식별자> --reason ...`: status를 `REVIEW_REJECTED`로 바꾸고 사유를 기록한다. AI는 이 사유를 반영해 03(또는 03c/03e)부터 다시 작성한다.
  - `review_cli.py status [<식별자>]` / `run_state.py list`: 진행 중인 학생들의 현재 단계를 한눈에 확인한다.
- 우선순위 2(선택, 보류): `http.server` 기반 로컬 HTML 리뷰 페이지. 여러 학생을 목록으로 보여주고 diff를 시각화하는 형태로, 필요성이 확인되면 추가한다.
- 승인 이후 리뷰 화면은 종료되고, 다음 `Linter 실행 승인` 게이트와는 별개의 확인으로 유지한다(중복 승인이지만 각각 성격이 다름: 내용 검토 vs 규정 검증).
- `AGENTS.md`의 "2-A. Human-in-the-Loop 체크포인트"에 A·B·C·E모드가 이 도구를 언제 호출하는지, 04 실행 전 상태를 어떻게 확인하는지, 완료 후 체크포인트 파일을 어떻게 정리하는지 구체적으로 정의했다.

### 6.5 실행 단계 및 현재 진행 상태

| Phase | 내용 | 상태 |
| --- | --- | --- |
| 0 | 3버전 구도 확정 및 문서 반영 | **완료** (본 문서) |
| 1 | A버전 동결 (`version_A/` 확인, git 태그) | **완료** (`v1.0.2-a-frozen`) |
| 2 | B버전 체크포인트 상태 기계 설계 | **완료** (6.3) |
| 3 | 로컬 리뷰 화면(CLI) 구현 | **완료** (`scripts/run_state.py`, `scripts/review_cli.py`, `tests/test_run_state.py`) |
| 4 | `.agent/02~04` 역할 경계 재정리 + HITL을 파이프라인에 실제 연결 | **완료** (`AGENTS.md` 2-A, `.agent/02~04`·`03c`·`03e`의 역할 표기) |
| 5 | Linter 재검사 루프를 상태 기계에 통합 | **완료** (`AGENTS.md` 5·6번의 `set-linted`/체크포인트 정리 절차) |
| 6 | 테스트 확장 (체크포인트/재개 시나리오) | **완료** — `tests/test_run_state.py`(15개), `tests/test_activity_and_physics_instructions.py`(신규, 01c/01d/01e/01f/03c/03e 커버), `tests/test_master_rules.py`에 HITL 통합 검증 6개 추가. 기존에 깨져 있던 `test_all_pipeline_files_exist`·`test_report_ingestion_uses_portable_utf8_extractor`도 함께 정리해 전체 91개 테스트 통과 |
| 7 | 문서·배포 스크립트 업데이트 | **완료** — `build_release.ps1`·릴리스 레이아웃 테스트 반영, `사용안내.md`에 "4-1. 초안 검토" 신설, `README.md`에 run_state·review_cli 안내 추가, `VERSION`을 0.3.0으로 상향 |
| 8 | 파일럿 운영 및 피드백 반영 | **실행 보류 — 이 세션에서 대신할 수 없음.** 실제 교사가 review_cli 흐름을 며칠간 써 보고 반려율·리뷰 소요 시간·인터럽트 지점(초안 직후가 맞는지)에 대한 피드백을 주는 절차이므로 코드 작업이 아니라 운영 활동이다. 아래 "8. 파일럿 체크리스트" 참고 |

### 6.6 Phase 8 파일럿 체크리스트 (실행은 보류, 준비만 기록)

Phase 8은 코드가 아니라 실제 교사의 사용 경험이 필요한 단계라 이 세션에서 대신 수행할 수 없다. 착수할 때 아래를 확인한다.

1. **배포**: `scripts/build_release.ps1`로 `Setuk-Harness-0.3.0.zip`을 만들어 파일럿 참여 교사에게 전달한다.
2. **범위**: 처음에는 한두 명, 실제 학생 데이터가 아닌 `학생정보/example.yaml` 같은 가상 사례로 리뷰 화면(`open`/`approve`/`reject`) 흐름부터 익히게 한다.
3. **관찰 항목**:
   - 초안 검토 인터럽트 지점이 적절한가(03 직후가 맞는지, 04 이후가 나은지)
   - `review_cli.py` 명령을 오타 없이 따라 칠 수 있는가, PowerShell 명령이 너무 부담스럽지 않은가
   - 반려(`reject`) 후 재작성 루프가 실제로 유용한가, 아니면 그냥 직접 고치는 쪽을 선호하는가
   - run_state 정리(완료 후 삭제)가 예상대로 동작하는가, 중간에 채팅이 끊겼을 때 재개가 되는가
4. **피드백 수집 후**: 이 문서 6.5의 Phase 8 행을 업데이트하고, 필요하면 6.4(리뷰 화면 요구사항)의 "우선순위 2" 로컬 HTML 화면 착수 여부를 재검토한다.

---

## 7. A버전 (version_A/) 요약

- 위치: `version_A/` 폴더, git 태그 `v1.0.2-a-frozen`으로 동결.
- 사용 환경: Claude Project 등 웹 지식 베이스에 `00_master_instructions.txt` + `01~04` + `rules.json` + `cloud_linter.py`를 함께 업로드하고 채팅으로 사용.
- B버전과의 핵심 차이: DOCX 자동 수집 단계가 없고(파일을 채팅에 직접 업로드), `cloud_linter.py`를 Analysis Tool로 웹 환경 안에서 실행하며, `loop_count` 공유 카운터로 재작성을 최대 2회까지 자동 반복한다(B버전은 사용자가 매 재검사마다 승인).
- 자기완결적 폴더로 확인됨 — 내부에서 `python_portable`, 루트 `linter.py`/`rules.json` 등 바깥 경로를 참조하지 않는다. 따라서 B버전 고도화가 A버전에 영향을 주지 않는다.
- 이후 A버전에 대한 수정은 별도로 "A버전 유지보수" 요청이 명시된 경우에만 진행한다.

## 8. C버전 (Phase 9, v1 구현 완료)

C버전은 Setuk-AI 코드가 Anthropic API 키로 **직접** LLM을 호출하는, 완전히 별도의 배포물이다. 위치: `version_C/` 폴더. 여기서만 `langgraph`·`anthropic`·`flask`가 실제 의존성으로 들어간다.

착수 당시 원래 계획은 "B버전이 Phase 8(파일럿)까지 안정화된 뒤" 시작하는 것이었으나, 사용자 판단으로 그 순서를 건너뛰고 바로 구현했다(2026-09-04). Phase 8 파일럿에서 나올 피드백(인터럽트 지점 적절성 등)은 아직 반영되지 않은 상태로 남아 있다.

### 8.1 확정된 결정

- **실행 형태**: 로컬 웹 UI/서버(Flask). 터미널 명령이 아니라 브라우저에서 초안을 검토·승인·반려한다.
- **API 키 관리**: 교사 개인 키를 `version_C/.env`에 저장(`version_C/.env.example` 템플릿 제공, `.env` 자체는 `.gitignore` 처리).
- **무설치 원칙 포기**: 이미 합의된 대로 `pip install -r version_C/requirements.txt` 필요. 포터블 Python은 쓰지 않는다.
- **멀티에이전트 구성**: 별도 모델을 쓰는 진짜 이종 에이전트가 아니라, 같은 Claude 모델(기본 `claude-opus-5`, `.env`에서 교체 가능)을 `.agent/01~04.md` 지침으로 역할만 나눠 순서대로 호출하는 방식. `.agent/*.md`를 B버전과 **그대로 공유**해 두 버전의 판단 기준이 갈라지지 않게 했다(`version_C/prompts.py`).
- **Human-in-the-Loop**: 실제 `langgraph`의 `interrupt()`/`Command(resume=...)`로 구현(기획서 초안이 원래 의도한 대로). `langgraph-checkpoint-sqlite`로 상태를 저장해 서버 재시작 후에도 진행 중이던 작업을 이어갈 수 있다.
- **검증 로직 재사용**: `linter.py`·`scripts/extract_docx.py`를 import로 그대로 재사용(`version_C/bridge.py`). B/C버전이 서로 다른 검증 기준을 갖지 않는다.

### 8.2 구현된 범위 (v1)

`version_C/` 아래 `bridge.py`(linter·extract_docx 재사용) · `prompts.py`(.agent/*.md 로더) · `llm.py`(Claude 호출·yaml 계약 파싱) · `pipeline.py`(LangGraph 그래프: ingest → structure → draft → **human_review(interrupt)** → evaluate → lint → fix 재시도) · `server.py`(Flask 라우팅·검토 화면) · `run_server.py`(진입점)로 구성했다. 그래프는 03(초안) 직후 멈추고, 교사가 웹 화면에서 승인하면 04(평가)로, 반려하면 사유를 반영해 03으로 되돌아간다. Linter 실패 시 AGENTS.md 5번과 동일하게 최대 5회까지 스스로 고쳐 재검사한다.

테스트는 `version_C/tests/`(시스템 Python 환경, v1 기준 21개 → v2에서 81개, 8.3 참고)에 있으며, **`version_B/tests/`(포터블 Python, B버전 전용)와는 완전히 분리**했다 — 포터블 Python에는 `langgraph`/`anthropic`이 없고 앞으로도 넣지 않을 것이므로, 같은 폴더에 두면 B버전 테스트 스위트가 매번 임포트 오류로 깨진다.

### 8.3 v2 신뢰성 개선 (2026-09-10)

v1을 실제 플랫폼으로 쓸 수 없게 막던 문제 네 가지를 정리했다. 화려한 기능 추가가 아니라
"이 구조를 신뢰할 수 있는가"에 대한 답이다.

**(1) 재시작하면 재개할 수 없던 문제.** v1은 LangGraph 체크포인트를 SQLite에 저장하면서도
웹 작업 목록은 프로세스 메모리(`_RUNS`)에 뒀다. 서버를 껐다 켜면 체크포인트가 남아 있어도
UI가 `thread_id`를 몰라 그 작업에 도달할 수 없었다 — 사실상 재개 불가였다.

- `runs`·`reviews`·`artifacts`·`audit_logs` 4개 테이블을 SQLAlchemy로 정의하고
  **`run.id`를 LangGraph `thread_id`와 같은 값**으로 고정해 두 저장소가 항상 같은 작업을
  가리키게 했다. 스키마는 Alembic으로 관리한다(`alembic upgrade head`).
- `DATABASE_URL`이 PostgreSQL이면 작업 DB와 LangGraph 체크포인터(`PostgresSaver`)가 함께
  전환된다. 미설정이면 로컬 SQLite — 혼자 쓰는 교사가 DB를 설치할 이유는 없기 때문이다.

여기서 나온 실패와 대응:

| 실패 | 대응 |
| --- | --- |
| 프로세스가 죽어 작업이 `running`으로 영구히 굳음 | 리스(lease) + 복구 워커가 `interrupted`로 회수, 화면에서 재개 |
| 승인 버튼 중복 클릭으로 resume이 두 번 실행 | `(run_id, idempotency_key)` 유니크 제약 |
| 오래된 화면에서 승인해 최신 초안을 덮어씀 | 초안 해시 대조 + `version` 컬럼 낙관적 잠금 |
| run 행은 있는데 체크포인트가 없어 `KeyError` | 재개 전 체크포인트 존재 확인, 원인을 설명하는 오류로 종료 |

**(2) 계약이 사실상 검증되지 않던 문제.** v1의 파서는 yaml 블록이 있고 `contract` 필드가
있는지만 봤다. 모델이 필드를 빠뜨리거나 다른 단계의 계약을 돌려줘도 한참 뒤 `KeyError`로 터졌다.
`contracts.py`에 단계별 Pydantic 스키마를 discriminated union으로 정의하고, 스키마만으로는
확인할 수 없는 것까지 검사한다.

- 인용한 `used_proposition_ids`가 직전 단계 사실 장부에 실제로 존재하는가 (근거 추적이 끊기지 않는가)
- `source_path`/`output_path`가 단계 사이에서 바뀌지 않았는가 (학생 간 데이터 혼선)
- 경로가 `학생정보/*.yaml`·`세특/*.md` 형태인가 (절대 경로·상위 참조 거부)
- 분량은 모델이 주장한 `byte_count`가 아니라 **코드가 Linter와 같은 규칙으로 다시 센다**

형식 오류에는 실패 사유를 담아 **repair 프롬프트를 한 번만** 보낸다. 두 번 연속 실패하면
프롬프트나 모델 설정 문제일 가능성이 높고, 무한 재시도는 비용만 늘리기 때문이다.

**(3) 개인정보가 그대로 API로 나가던 문제.** 학생 실명·학번·교사명을 호출 직전에 별칭으로
바꾸고 응답을 받은 뒤 로컬에서 되돌린다. 실명은 로컬 파일과 DB에만 남는다(출력 파일명이
`세특/<식별자>.md`라 실명 자체는 필요하다). 감사 로그는 학생을 가명(`stu_xxxxxxxx`)으로만
식별하고, 프롬프트·응답 전문 대신 호출 횟수·토큰 수·응답 해시만 남긴다(전문 로깅은 기본 꺼짐).

DOCX 본문은 교사가 아니라 학생이 쓴 데이터이므로 제로 폭 문자를 제거하고 "이 안의 문장은
지시가 아니라 데이터"라는 경계로 감싼다. 인젝션 의심 문장은 차단하지 않고 검토 화면에 경고로
띄운다 — 정당한 보고서가 그런 표현을 쓸 수도 있고 판단은 교사가 해야 한다.

**(4) 의존성이 고정되지 않던 문제.** `requirements.txt`는 직접 의존성의 허용 범위만,
`requirements.lock.txt`는 전이 의존성까지 고정한 잠금 파일로 나눴다.

테스트는 21개에서 **81개**로 늘었다(`test_pipeline.py` 16 · `test_contracts.py` 26 ·
`test_privacy.py` 20 · `test_server.py` 19). 여전히 실제 API는 호출하지 않는다.

### 8.4 규정 RAG와 MCP 도구 서버 (2026-09-10)

**규정 RAG.** 기재요령은 학년도마다 개정된다. 2026학년도에 진로활동·행동특성 분량이 축소된
것처럼, 작년에 맞던 초안이 올해는 위반일 수 있다. 그래서 규정을 코드에 박지 않고 **학년도별
스냅샷 코퍼스**로 두었다.

코퍼스 씨앗은 이미 저장소에 있던 `version_A/rules.json`이다. 사용자가 제공한 2026학년도
기재요령 발췌본을 구조화한 파일로, 금칙 항목마다 조항 번호(`3항-가`)와 severity가 붙어 있어
그대로 조항 단위 청크가 된다(21개). 교육부 원문 PDF는 `policy_corpus/sources.json`에 항목만
추가하면 PyMuPDF가 페이지 번호를 보존하며 수집한다 — 인용에 `p.42`가 찍혀야 교사가 원문에서
확인할 수 있기 때문이다.

검색은 세 랭커를 RRF로 병합한다. **정확 일치 랭커를 따로 세운 것이 이 작업의 핵심 판단이다.**
처음에는 BM25만 썼는데 "세특에 토익 점수를 써도 되나"라는 질의에서 공인어학시험 조항이
3위로 밀렸다 — "점수"·"성적"을 공유하는 모의고사 조항이 위로 올라온 것이다. 이 도메인은
금칙 용어와 조항 번호의 정확 일치가 결정적이므로 별도 랭커로 세워 RRF에 넣었고 1위로 올라왔다.
가중합이 아니라 RRF를 쓴 이유는 BM25 점수와 코사인 유사도의 스케일이 달라 코퍼스가 바뀔
때마다 가중치를 다시 튜닝해야 하기 때문이다.

dense 검색은 **선택 사항**으로 두었다. Anthropic이 임베딩 API를 제공하지 않아 키가 하나 더
필요한데, 규정 검색은 정확 일치 질의가 대부분이라 없이도 실용적이다. 켜면 자동으로 hybrid가
되고, 임베딩 호출이 실패하면 조용히 sparse로 폴백한다.

인용 정책에서 한 번 방향을 바꿨다. 처음에는 Linter 진단마다 규정을 **검색해서** 근거를
붙였는데, "제한"·"기재" 같은 흔한 단어를 공유하는 엉뚱한 조항이 달렸다(분량 진단에 인증시험
조항이 붙었다). 진단 코드와 조항의 관계는 대부분 결정적이므로 — `BYTE_LIMIT`은 언제나 해당
기재 항목의 분량 조항, `FORBIDDEN_TERM`은 그 단어를 금칙어로 가진 조항 — **직접 조회로
바꿨다.** 알 수 없는 경우(탭 문자처럼 기재요령이 아니라 NEIS 입력 제약인 진단)는 빈칸으로
둔다. 틀린 근거를 붙이면 나머지 인용까지 믿을 수 없게 되기 때문이다.

부수 효과가 하나 있다. `version_B/rules.json`의 금칙어는 2개뿐이지만 코퍼스에는 발췌본의
금칙 항목 전체가 있다. B의 검사 계약을 건드리지 않으면서 **자문 성격의 추가 점검**을 붙였다
— B의 Linter가 통과시키는 "장학금을 받음"을 C는 `3항-카` 근거로 기재 불가로 표시한다.
통과/실패 판정은 바꾸지 않으므로 두 버전의 계약은 여전히 같다.

**MCP 도구 서버.** B버전은 IDE 에이전트가 `AGENTS.md`를 읽고 자연어 지시로 파일을 직접
다룬다. 편하지만 어떤 파일을 읽고 쓸지에 대한 통제가 프롬프트에만 있다. 학생 개인정보를
다루는 도구에서는 충분하지 않다. 같은 기능을 MCP로 노출하면 **타입이 정해진 호출**로 제한된다.

7개 도구(`extract_docx`·`validate_student_yaml`·`search_school_policy`·`lint_record`·
`get_run_status`·`get_evidence_for_sentence`·`submit_review`)를 열되, 정책을 프롬프트가
아니라 코드에 두었다.

| 정책 | 구현 |
| --- | --- |
| 경로를 인자로 받지 않음 | `student_key`·파일명만 받고 경로는 코드가 조립 |
| 읽기/쓰기 분리 | `submit_review`만 부작용 있음 |
| 위험한 도구에 승인 요구 | 쓰기 도구는 `SETUK_MCP_ALLOW_WRITE=1` 없이 거부 |
| 감사 로그 | 모든 호출 기록(인자 내용이 아니라 키 이름만) |
| 호출 상한 | 세션당 200회, 무한 에이전트 루프 차단 |
| 인젝션 격리 | 문서 본문을 untrusted 경계로 감싸서 반환 |

`submit_review`를 기본 차단한 것이 중요한 결정이다. MCP 클라이언트는 대개 도구를 자동
승인하도록 설정할 수 있는데, 교사의 승인을 대신 기록하는 도구가 그렇게 자동 실행되면
**Human-in-the-Loop 자체가 무의미해진다.** 이 서버는 그래프를 재개하지도 않는다 — 승인으로
파이프라인이 실제로 진행되는 경로는 사람이 웹 화면에서 누르는 것 하나로 유지했다.

테스트는 81개에서 **146개**로 늘었다(`test_policy.py` 39 · `test_mcp.py` 26 추가).
MCP 서버는 실제 MCP 클라이언트로 stdio 핸드셰이크까지 확인했다.

### 8.5 계획 대조 감사와 보완 (2026-09-10)

8.3~8.4를 끝낸 뒤 `개선 계획.md`의 Setuk-AI 항목을 한 줄씩 다시 대조했다. "했다고 생각했지만
실제로는 안 된" 것이 여섯 가지 나왔다.

**(1) 리스가 하트비트 없이 한 번만 잡혔다 — 실제 버그.** 시작할 때 120초 리스를 잡고 끝날
때까지 갱신하지 않았다. 그러면 리스는 "살아 있다"가 아니라 "시작한 지 120초가 지났다"를
뜻할 뿐이다. 세특 한 건은 LLM을 3~4번 부르므로 120초를 넘기기 쉽다. 단일 프로세스에서는
소유자 검사(`lease_owner == WORKER_ID`)에 가려 드러나지 않지만, **PostgreSQL로 여러
인스턴스를 띄우는 순간 아직 돌고 있는 작업을 다른 워커가 죽은 것으로 보고 회수해 간다.**
PostgresSaver를 붙인 이유가 바로 다중 인스턴스인데 그 구성에서 깨지는 구조였다. 실행 중
리스를 주기적으로 갱신하는 `_LeaseHeartbeat`를 붙였다.

**(2) MCP 타임아웃이 상수만 있고 정책이 없었다.** `DEFAULT_TIMEOUT_SECONDS = 30`을 선언해
두고 아무 데서도 쓰지 않았다. 계획서의 "timeout과 최대 호출 횟수" 중 호출 횟수만 지켜진
셈이다. `asyncio.wait_for`로 실제로 끊게 했다. 다만 파이썬은 실행 중인 스레드를 죽일 수
없으므로 이것이 끊는 것은 **클라이언트의 대기이지 작업 자체가 아니다.** 응답 메시지에 그
사실을 그대로 적었다.

**(3) 도구 출력에 스키마가 없었다.** 계획서는 "tool input/output JSON Schema"인데 입력만
있었다. 7개 도구 전부에 `outputSchema`를 붙였고, MCP SDK가 `structuredContent`를 그 스키마로
검증하므로 도구가 약속과 다른 모양을 내면 클라이언트가 아니라 서버에서 걸린다.

**(4) 보존 기간 설정이 없었다.** `retention.py`로 초안·산출물의 보존 기간을 두되 기본은
끔으로 했다(학교마다 정책이 다르다). **감사 로그는 기본 보존 대상에서 뺐다** — 삭제했다는
사실 자체를 증명하는 기록이라 함께 지우면 증명할 수 없기 때문이다.

**(5) 학생별 데이터 삭제 API가 없었다.** `/privacy`에서 가명으로 지정해 삭제한다. 삭제
요청에 실명을 쓰지 않아도 되게 한 것이 요점이다. 초안 본문을 비울 때 run 행 자체는 남긴다
— 행이 사라지면 감사 로그의 `run_id`가 미아가 된다.

**(6) 6장의 Agent/HITL 지표를 집계하는 곳이 없었다.** 원자료는 다 쌓고 있었지만 아무도
읽지 않았다. `/metrics`에서 성공률·반려율·**초안 대비 교사 수정 거리**·초안→승인 소요
시간·단계별 실패율·복구 성공률을 계산한다. 수정 거리는 이 프로젝트가 애초에 HITL을 넣은
근거("교사가 결과물을 자주 고친다")를 수치로 확인하는 지표라 특히 중요하다.

지표 설계에서 한 가지를 의식적으로 지켰다. **모든 비율을 분모와 함께 보고한다.** 작업이
2건일 때의 "반려율 100%"를 그냥 100%로 적으면 그 수치는 거짓말이 된다. 표본이 없으면
0%가 아니라 `—`로 둔다 — 0%는 "실패가 없다"로 읽히기 때문이다.

함께 붙인 것: 작업당 토큰 상한(`SETUK_MAX_TOKENS_PER_RUN`). 반려 재작성과 Linter 재시도가
겹치면 한 학생에 LLM을 십수 번 부를 수 있는데, 교사 개인 키로 도는 구조라 예상치 못한
청구서가 곧 서비스 중단이다.

감사에서 **문제없음으로 확인된 것**도 적어 둔다. LLM이 만든 Markdown을 화면에 뿌리는
XSS 경로는 Flask의 `render_template_string`이 자동 이스케이프하므로 이미 막혀 있었다
(실제로 `<img src=x onerror=...>`를 넣어 확인했다).

테스트는 146개에서 **192개**로 늘었다(`test_governance.py` 31 신설, MCP·서버 테스트 15 추가).

### 8.6 두 번째 계획 문서와의 대조 — 실제 모순 수정 (2026-09-11)

`개선 계획 세부.md`라는 더 구체적인 두 번째 계획 문서가 뒤늦게 채워졌다. 8.5의 감사는
`개선 계획.md` 기준이었는데, 이 문서와 다시 대조하니 **권고가 아니라 실제로 반대로
구현한 것**이 두 가지 나왔다.

**(1) "알 수 없는 필드 거부"를 정반대로 구현했었다.** `contracts.py`는 `extra="allow"`로
모델이 스키마 밖 필드를 끼워 넣어도 통과시켰다. 당시엔 "지침이 필드를 추가해도 파이프라인이
안 깨지게"라는 나름의 이유였지만, 계획 문서는 정반대를 요구했다 — 계약의 요점은 무엇이
올지 미리 정하는 것이고, 느슨하게 두면 `confidence: 0.9`처럼 아무도 검증하지 않는 값이
하류에서 근거인 척 쓰일 수 있다. `extra="forbid"`로 바꾸기 전에 `.agent/01~04.md` 네 개
지침의 계약 블록에서 실제 필드명을 전부 추출해(`grep`으로 기계적으로) 스키마와 대조했다
— 잠그기만 하고 지침에 있는 필드를 빠뜨리면 파이프라인이 첫 실행부터 멈추기 때문이다.
`subject`·`constraints`·`target_byte_range`·`review`처럼 그동안 dict로 느슨하게 받던
필드도 전용 모델로 승격했다.

**(2) "복구 실패 시 NEEDS_INPUT 또는 사용자 검토로 전환"이 안 지켜지고 있었다.** repair
1회까지 시도하고 실패하면 4단계 전부 `{"error": ...}`로 런 자체를 끝냈다. HITL 프로젝트에서
사람이 검토할 기회 없이 작업이 죽는 것은 설계 취지와 어긋난다. `error`(코드가 처리할 수
없는 고장 — 잘못된 경로, 파일 없음)와 `needs_input`(사람이 채우면 풀리는 공백 — 계약 복구
실패, 모델이 선언한 정보 부족)을 DB 컬럼과 상태값(`STATUS_NEEDS_INPUT`)으로 분리하고,
검토 화면에 중단 단계·질문 목록·실패 사유를 그대로 보여 주게 했다.

여기서 실제 버그를 하나 더 잡았다. `needs_input`의 종류(질문 대기 vs 계약 검증 실패)를
처음엔 `questions` 목록이 비었는지로 추론했는데, 모델이 `NEEDS_INPUT_V1`을 **빈 질문
목록과 함께** 돌려주면 정보 부족이 형식 실패로 잘못 분류됐다. 실제로 기존 테스트
(`test_structure_needs_input_short_circuits_without_drafting`)를 needs_input 기준으로
고치는 과정에서 이 오분류가 드러났다. `kind`를 호출부가 명시하도록 고쳤다.

**(3) 계획 문서의 "실행 결과 크기 제한"도 없었다.** MCP 도구, 특히 `extract_docx`가 큰
DOCX를 그대로 돌려주면 에이전트 컨텍스트를 한 번에 태운다. 도구별로는 본문을 앞부분만
자르고 **잘린 사실을 본문 안에 명시**하며(조용히 자르면 에이전트가 문서 전체를 봤다고
착각한다), 그걸 빠져나온 경우에 대비해 `call_tool`이 전체 응답 크기를 마지막으로 한 번
더 재서 넘으면 잘라 내지 않고 거부한다.

세 항목 모두 실제 그래프 실행으로 검증했다 — 특히 (2)는 repair가 반복 실패하는 상황을
가짜 응답으로 재현해 `error`가 `None`이고 `needs_input`만 채워진 채 03단계로 넘어가지
않는 것을 확인했다.

테스트는 192개에서 **206개**로 늘었다.

### 8.7 아직 없는 것 (다음에 할 일)

- **세특(A·B에 해당) 단일 보고서/YAML 흐름만** 구현했다. 창체(C)·물리 통합(E)·땜빵모드(F)는 없다.
- 비용 모델(교사 개인 부담 vs 학교/교육청 예산)은 "개인 키"로 확정됐을 뿐 실제 정산·상한 안내 같은 운영 정책은 없다.
- 여러 교사가 동시에 쓸 때의 **인증이 없다**(단일 로컬 사용자 전제). 이것 하나 때문에 계획서 4장의 "교사·학교 tenant 분리", 6장 보안 테스트의 "다른 사용자의 report/run 접근"이 함께 미해결로 남아 있다. 검토자 식별자와 감사 로그는 남기지만 그 값을 검증하지 않는다. 지금 구조에서 서버에 접근할 수 있는 사람은 모든 학생의 초안을 볼 수 있으므로, 학교 단위 배포 전에 인증·RBAC·테넌트 분리를 먼저 붙여야 한다.
- 규정 코퍼스에 들어 있는 것은 **사용자 제공 발췌본**이지 교육부 원문 PDF가 아니다. 원문을 넣으면 페이지 인용까지 가능하지만 아직 넣지 않았다.
- 진로활동·행동특성의 2026학년도 축소 분량은 수치가 확인되지 않아 코퍼스에도 '미확인'으로 들어 있다. 확정 수치를 확인해 갱신해야 한다.
- dense 검색은 구현했으나 임베딩 제공자를 붙여 실제로 켜 본 적은 없다(기본 sparse-only).
- Phase 8 파일럿을 건너뛰고 만들었으므로, B버전 파일럿에서 나올 "인터럽트 지점이 적절한가" 같은 피드백이 아직 이 구현에 반영되지 않았다.

사용법은 `version_C/README.md` 참고.

---

## 9. 저장소 재구성 (2026-09-04)

세 버전을 저장소 루트 바로 아래 `version_A/`·`version_B/`·`version_C/`로 각각 옮겨 이름을 통일했다. 이전에는 A버전이 `V1 0.2/`(과거 명칭이 남은 폴더), B버전이 저장소 루트 자체, C버전이 `c_version/`으로 서로 다른 규칙의 이름을 쓰고 있었다.

- `V1 0.2/` → `version_A/` (내용 변경 없음)
- 저장소 루트에 있던 B버전 파일 전체(`.agent/`, `.clinerules/`, `AGENTS.md`, `linter.py`, `rules.json`, `scripts/`, `python_portable/`, `tests/`, `학생정보/`, `보고서/`, `세특/`, `창체/`, `run_state/`, `VERSION`, `사용안내.md`, `LICENSE`, `THIRD_PARTY_NOTICES.md`, 기존 `README.md`) → `version_B/` 아래로 이동
- `c_version/` → `version_C/` (내용은 동일하나, `version_C/bridge.py`가 `linter.py`·`.agent/*.md`를 찾는 기준 경로를 `version_B/`로 다시 잡도록 수정했다 — B의 파일들이 더 이상 저장소 루트에 없기 때문이다. `run_server.py`와 `tests/`의 `from c_version import ...`도 `from version_C import ...`로 바꿨다)
- 저장소 루트에는 이제 `.gitignore`와 세 버전을 아우르는 문서(`기획서.md`, `개선 계획.md`, 새로 쓴 안내용 `README.md`)만 남는다.

**개발 시 주의할 점**: 배포되는 B버전 ZIP의 내용물 구조(교사가 여는 `Setuk-Harness-<버전>/` 폴더 내부)는 이 변경으로 전혀 바뀌지 않는다 — `scripts/build_release.ps1`이 `version_B/`를 기준으로 여전히 같은 모양으로 압축한다. 다만 **이 저장소에서 B버전 AI 파이프라인을 직접 실행하며 개발할 때는(`AGENTS.md`를 인식시켜야 하므로) VSCode에서 저장소 루트가 아니라 `version_B/` 폴더 자체를 열어야 한다** — Claude Code/Cline 같은 도구는 워크스페이스 루트에서 `AGENTS.md`/`.clinerules/`를 찾기 때문이다.
